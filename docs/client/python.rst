=====================
Python client library
=====================

To handle connection problems or remote exceptions the client can raise two types of errors: ``ConnectionException`` and ``RemoteException``. For error handling see more in

